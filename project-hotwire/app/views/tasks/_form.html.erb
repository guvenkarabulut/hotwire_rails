<%= form_with(model: [@project, Task.new]) do |f| %>
  <div class="card card-body bg-info text-white">
    <%= f.text_area :body, class:"form-control", required: true %>
    <%= f.select(:status , [['TODO'],['On-progress'],['Completed']]) %>
    <%= f.submit %>
  </div>
<% end %>
